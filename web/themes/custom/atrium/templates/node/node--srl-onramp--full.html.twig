{% extends 'node.html.twig' %}
{% block content %}

  {% include '@molecules/blocks/hero/hero.twig' with {
    breadcrumbs: [{ label: label }],
    title: content.field_title_display|render ? content.field_title_display : label,
    body: content.field_description|render ? content.field_description : '',
    column_content_right: header_node|render,
    column_variant: header_node|render ? 'has-two-columns-threequarter' : ''
  } %}

  {{ content|without(
    'field_case_type',
    'field_description',
    'field_hero_nav_title',
    'field_title_display'
  )}}
{% endblock %}
